# --- Local configuration (real DB credentials) — never commit ---
app/config.php

# --- Runtime uploads (resident issue photos) ---
app/public/uploads/*
!app/public/uploads/.gitkeep

# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# --- Dependencies (if added later) ---
vendor/
node_modules/
